# Local only — copy to .env (never commit .env)
VITE_API_URL=http://localhost:3000/api

# Production (Vercel): set the same key to your live API, e.g.
# VITE_API_URL=https://helpdesk-api.onrender.com/api
